% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastClusGap.R
\name{fastClusGap}
\alias{fastClusGap}
\title{Drop-in replacement for cluster::clusGap() when d.power = 2}
\usage{
fastClusGap(
  x,
  K.max,
  B = 100L,
  nstart = 1L,
  iter.max = 30L,
  k.min = 1L,
  ncores = 1L,
  verbose = interactive()
)
}
\arguments{
\item{x}{numeric matrix or data frame (e.g. the retained PCA scores)}

\item{K.max}{largest k to test}

\item{B}{number of uniform reference datasets (clusGap's B / random_set)}

\item{nstart}{passed to kmeans()}

\item{iter.max}{passed to kmeans()}

\item{k.min}{smallest k to test. clusGap always starts at 1; k = 1 is the
single most expensive call there and is usually uninformative,
so k.min = 2 is a cheap saving. SEE THE WARNING ON selectK().}

\item{ncores}{number of cores for the reference-dataset loop, which is
embarrassingly parallel. Default 1 (serial). Uses
parallel::mclapply(), so Unix-alikes only; on Windows it
warns and runs serially. Results do NOT depend on ncores:
each reference sample draws from its own pre-generated seed,
so serial and parallel runs are bit-identical.}

\item{verbose}{progress tracing in the style of cluster::clusGap(), plus
an ETA printed after the first bootstrap sample. Defaults
to interactive(), matching clusGap's own default.}
}
\value{
matrix with columns logW, E.logW, gap, SE.sim (as clusGap()$Tab) plus
an attribute "k" giving the k value of each row.
}
\description{
Exact replacement for \code{cluster::clusGap()} for the \code{FUNcluster = kmeans},
\code{d.power = 2} case used by \code{customKmeans()}. \code{clusGap()} computes \code{W_k} by
building the full pairwise distance matrix of every cluster, for every
candidate \code{k}, for the real data and for each of the \code{B} reference
datasets -- O(n^2) in time and memory. This function instead uses the
identity \code{W_k = 0.5 * kmeans(...)$tot.withinss} (see \code{\link[=Wk_fast]{Wk_fast()}}), which
\code{kmeans()} has already computed, making the whole call linear in \code{n}. The
identity holds only for \code{d.power = 2} -- see \code{\link[=checkWkIdentity]{checkWkIdentity()}} to verify
it on your own data. Returns the same four-column table as
\code{clusGap()$Tab}; read the cluster count off it with \code{\link[=selectK]{selectK()}}, not
\code{cluster::maxSE()} directly.
}
\examples{
set.seed(1)
x <- rbind(matrix(rnorm(100, sd = 0.3), ncol = 2),
           matrix(rnorm(100, mean = 4, sd = 0.3), ncol = 2))
Tab <- fastClusGap(x, K.max = 5, B = 20, verbose = FALSE)
selectK(Tab)

}
