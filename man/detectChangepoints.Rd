% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breakpoints.R
\name{detectChangepoints}
\alias{detectChangepoints}
\title{Detect and classify candidate regime-change breakpoints in a time series}
\usage{
detectChangepoints(
  dates,
  x,
  min_segment_length = 12,
  penalty_factor = "BIC",
  smooth_span = 0.2,
  change_threshold = 0.03,
  slope_p_threshold = 0.1,
  slope_magnitude_threshold = 0.05
)
}
\arguments{
\item{dates}{Date vector.}

\item{x}{Numeric vector, same length as \code{dates}; the variable to detect
changepoints in.}

\item{min_segment_length}{Integer; minimum segment length for \code{cpt.mean()}
and for a series to be analysed at all (needs > \code{2 * min_segment_length}
points). Default 12.}

\item{penalty_factor}{Passed to \code{cpt.mean(penalty = )}. Default \code{"BIC"}.}

\item{smooth_span}{LOESS span. Default 0.2.}

\item{change_threshold}{Numeric; minimum proportional change between two
stable segments to count as a candidate breakpoint. Default 0.03.}

\item{slope_p_threshold, slope_magnitude_threshold}{Thresholds for
classifying a segment as increasing/decreasing vs stable. Defaults 0.1,
0.05.}
}
\value{
\code{NULL} if there isn't enough data; otherwise a list with
\code{bp_dates} (Date vector of candidate breakpoints, sorted) and
\code{segment_info} (data frame describing every segment between raw
changepoints -- kept for \code{\link[=plotBreakpoints]{plotBreakpoints()}}'s segment-classification
overlay, see note there).
}
\description{
Smooths the series with LOESS, runs \code{changepoint::cpt.mean()} (PELT) to
find raw changepoints, classifies the segment between each pair of
consecutive changepoints as increasing/decreasing/stable (via segment
slope significance and magnitude), and keeps only transitions between
stable segments whose means differ by more than \code{change_threshold}
(proportionally). Breakpoints within one year of the series end are
dropped. No domain (age/spatial) validation happens here.
}
\seealso{
Other breakpoint analysis utilities:
\code{\link[=ageFromLength]{ageFromLength()}},
\code{\link[=aggregateClusterTimeSeries]{aggregateClusterTimeSeries()}},
\code{\link[=analyseBreakpoints]{analyseBreakpoints()}},
\code{\link[=calculateClusterMovementBaseline]{calculateClusterMovementBaseline()}},
\code{\link[=calculateDistance]{calculateDistance()}},
\code{\link[=calculateFleetSpread]{calculateFleetSpread()}},
\code{\link[=calculateSpatialOverlap]{calculateSpatialOverlap()}},
\code{\link[=evaluateBreakpointMovement]{evaluateBreakpointMovement()}},
\code{\link[=validateBreakpointAge]{validateBreakpointAge()}},
\code{\link[=validateBreakpointSpatial]{validateBreakpointSpatial()}}
}
\concept{breakpoint analysis utilities}
