% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastClusGap.R
\name{Wk_fast}
\alias{Wk_fast}
\title{Compute W_k for one candidate cluster count}
\usage{
Wk_fast(X, k, nstart = 1L, iter.max = 30L)
}
\arguments{
\item{X}{numeric matrix or data frame.}

\item{k}{integer; number of clusters.}

\item{nstart}{passed to \code{\link[stats:kmeans]{stats::kmeans()}}.}

\item{iter.max}{passed to \code{\link[stats:kmeans]{stats::kmeans()}}.}
}
\value{
Numeric scalar, \code{W_k} on its original scale (not logged; callers
take \code{log()} of the result themselves, see \code{\link[=fastClusGap]{fastClusGap()}}).
}
\description{
Internal helper for \code{\link[=fastClusGap]{fastClusGap()}}. Returns the pooled within-cluster
sum of squares that \code{cluster::clusGap()}'s internal \code{W.k()} computes from
the full pairwise distance matrix when \code{d.power = 2}, obtained instead via
the identity \code{W_k = 0.5 * kmeans(...)$tot.withinss}. At \code{k = 1} this is
computed directly (no clustering needed) as \code{0.5 * sum((X - colMeans(X))^2)}.
}
\keyword{internal}
