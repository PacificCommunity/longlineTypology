% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastClusGap.R
\name{checkWkIdentity}
\alias{checkWkIdentity}
\title{Verify the fastClusGap() W_k identity on your own data}
\usage{
checkWkIdentity(X, k = 5L, tol = 1e-08)
}
\arguments{
\item{X}{numeric matrix or data frame.}

\item{k}{integer; number of clusters to test. Default 5.}

\item{tol}{numeric; tolerance passed to \code{all.equal()}. Default 1e-8.}
}
\value{
Invisibly \code{TRUE} if the identity holds to within \code{tol}; stops
(via \code{stopifnot()}) otherwise. Also prints both computed values.
}
\description{
Self-test confirming, on the data supplied, that the pairwise-distance
computation \code{cluster::clusGap()} uses internally (\code{0.5 * sum(dist(x_i, mu_r)^2)} per cluster) matches \code{0.5 * kmeans(...)$tot.withinss} -- the
identity \code{\link[=fastClusGap]{fastClusGap()}} relies on to avoid ever building that distance
matrix. Cheap only for small \code{n}, since it calls \code{stats::dist()};
subsamples to at most 5000 rows if \code{X} is larger. Run once per
dataset/scenario before trusting \code{\link[=fastClusGap]{fastClusGap()}}'s output on it, not as
part of a routine pipeline.
}
\examples{
set.seed(1)
x <- matrix(rnorm(200), ncol = 2)
checkWkIdentity(x, k = 3)

}
