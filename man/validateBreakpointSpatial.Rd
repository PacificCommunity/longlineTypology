% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breakpoints.R
\name{validateBreakpointSpatial}
\alias{validateBreakpointSpatial}
\title{Test whether breakpoints coincide with the fleet relocating}
\usage{
validateBreakpointSpatial(
  bp_dates,
  df,
  cluster_id,
  strictness = "moderate",
  baseline = NULL
)
}
\arguments{
\item{bp_dates}{Date vector of candidate breakpoint dates.}

\item{df}{Data frame with \code{cluster}, \code{date}, \code{longitude}, \code{latitude}
columns (the raw, un-summarised fisheries data).}

\item{cluster_id}{Cluster identifier.}

\item{strictness}{Passed to \code{\link[=evaluateBreakpointMovement]{evaluateBreakpointMovement()}}. Default
\code{"moderate"}.}

\item{baseline}{Optional, pre-computed
\code{\link[=calculateClusterMovementBaseline]{calculateClusterMovementBaseline()}} result; if \code{NULL} (default) it is
computed here, once per call (no cross-call caching).}
}
\value{
A data frame, one row per element of \code{bp_dates} that had both a
before- and after-period in \code{df} (breakpoints with no following
observation are dropped -- matches the original, which never added such
a breakpoint to \code{valid_bp_dates}), with columns \code{bp_date},
\code{centroid_distance}, \code{spatial_overlap}, \code{spread_change_ratio}, \code{passed}.
}
\description{
For each candidate breakpoint, compares the fleet's position just before
and just after against the cluster's empirical movement baseline. A
breakpoint passes (is spatially valid) when the fleet did \emph{not} move
significantly -- a compositional shift explained by relocation isn't an
independent regime change.
}
\seealso{
Other breakpoint analysis utilities:
\code{\link[=ageFromLength]{ageFromLength()}},
\code{\link[=aggregateClusterTimeSeries]{aggregateClusterTimeSeries()}},
\code{\link[=analyseBreakpoints]{analyseBreakpoints()}},
\code{\link[=calculateClusterMovementBaseline]{calculateClusterMovementBaseline()}},
\code{\link[=calculateDistance]{calculateDistance()}},
\code{\link[=calculateFleetSpread]{calculateFleetSpread()}},
\code{\link[=calculateSpatialOverlap]{calculateSpatialOverlap()}},
\code{\link[=detectChangepoints]{detectChangepoints()}},
\code{\link[=evaluateBreakpointMovement]{evaluateBreakpointMovement()}},
\code{\link[=validateBreakpointAge]{validateBreakpointAge()}}
}
\concept{breakpoint analysis utilities}
