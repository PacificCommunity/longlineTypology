% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breakpoints.R
\name{validateBreakpointAge}
\alias{validateBreakpointAge}
\title{Test whether breakpoints are consistent with cohort ageing}
\usage{
validateBreakpointAge(
  bp_dates,
  dates,
  mean_len,
  series_end,
  window_months = 6,
  skip = FALSE,
  Linf = 150.3,
  K = 0.442,
  t0 = -0.244
)
}
\arguments{
\item{bp_dates}{Date vector of candidate breakpoint dates, sorted ascending.}

\item{dates}{Date vector for the full analysis series (aligned with
\code{mean_len}).}

\item{mean_len}{Numeric vector of mean length values, aligned to \code{dates}.}

\item{series_end}{Date; end of the analysis series, used as the phase end
for the last breakpoint.}

\item{window_months}{Numeric; months either side of a breakpoint used to
compute its implied age. Default 6.}

\item{skip}{Logical; if \code{TRUE}, bypasses the test -- every breakpoint
passes with \code{age_limit_days = Inf} (for variables with no length-age
relationship, e.g. \code{mean_hbf}). Default \code{FALSE}.}

\item{Linf, K, t0}{Growth parameters passed to \code{\link[=ageFromLength]{ageFromLength()}}.}
}
\value{
A data frame, one row per element of \code{bp_dates}, with columns
\code{bp_date}, \code{age_limit_days}, \code{phase_days}, \code{passed}.
}
\description{
For each candidate breakpoint, compares the time until the next breakpoint
(or the end of the series) against the age implied by mean length around
that breakpoint (\code{\link[=ageFromLength]{ageFromLength()}}). A breakpoint passes if the fleet
could plausibly still be fishing the same cohort at that point, i.e. the
phase length does not exceed the implied age.
}
\seealso{
Other breakpoint analysis utilities:
\code{\link[=ageFromLength]{ageFromLength()}},
\code{\link[=aggregateClusterTimeSeries]{aggregateClusterTimeSeries()}},
\code{\link[=analyseBreakpoints]{analyseBreakpoints()}},
\code{\link[=calculateClusterMovementBaseline]{calculateClusterMovementBaseline()}},
\code{\link[=calculateDistance]{calculateDistance()}},
\code{\link[=calculateFleetSpread]{calculateFleetSpread()}},
\code{\link[=calculateSpatialOverlap]{calculateSpatialOverlap()}},
\code{\link[=detectChangepoints]{detectChangepoints()}},
\code{\link[=evaluateBreakpointMovement]{evaluateBreakpointMovement()}},
\code{\link[=validateBreakpointSpatial]{validateBreakpointSpatial()}}
}
\concept{breakpoint analysis utilities}
