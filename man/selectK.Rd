% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastClusGap.R
\name{selectK}
\alias{selectK}
\title{Apply the Tibshirani (2001) SE-max rule to a fastClusGap() table}
\usage{
selectK(Tab, SE.factor = 1)
}
\arguments{
\item{Tab}{matrix returned by \code{\link[=fastClusGap]{fastClusGap()}}, with a \code{"k"} attribute (or,
failing that, \code{k} values given by \code{rownames(Tab)}).}

\item{SE.factor}{passed to \code{cluster::maxSE(..., SE.factor = )}. Default 1.}
}
\value{
Integer; the selected number of clusters.
}
\description{
Wraps \code{cluster::maxSE()} and returns the actual number of clusters \code{k},
not a row position. \code{cluster::maxSE()} returns a POSITION in the vector
it is given: \code{customKmeans()} gets away with
\code{nc <- cluster::maxSE(gap_stat$Tab[, "gap"], ...)} only because
\code{clusGap()} always starts at \code{k = 1}, so position and \code{k} coincide. If
\code{\link[=fastClusGap]{fastClusGap()}} was called with \code{k.min > 1} to skip the expensive \code{k = 1}
case, position and \code{k} differ by one, and calling \code{maxSE()} directly on
the table would silently return the wrong cluster count. Always go
through this function instead of calling \code{cluster::maxSE()} on the table
directly.
}
\examples{
set.seed(1)
x <- rbind(matrix(rnorm(100, sd = 0.3), ncol = 2),
           matrix(rnorm(100, mean = 4, sd = 0.3), ncol = 2))
Tab <- fastClusGap(x, K.max = 5, B = 20, verbose = FALSE)
selectK(Tab)

}
